import { hot } from 'react-hot-loader/root';
import * as React from 'react';
import Layout from './layout/Layout';

const Application = () => <Layout>Hello!</Layout>;

export default hot(Application);
